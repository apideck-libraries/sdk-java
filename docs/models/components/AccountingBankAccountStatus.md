# AccountingBankAccountStatus

The status of the bank account

## Example Usage

```java
import com.apideck.unify.models.components.AccountingBankAccountStatus;

AccountingBankAccountStatus value = AccountingBankAccountStatus.ACTIVE;

// Open enum: use .of() to create instances from custom string values
AccountingBankAccountStatus custom = AccountingBankAccountStatus.of("custom_value");
```


## Values

| Name       | Value      |
| ---------- | ---------- |
| `ACTIVE`   | active     |
| `INACTIVE` | inactive   |
| `CLOSED`   | closed     |