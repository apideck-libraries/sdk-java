# Audience

Audience for the doc.

## Example Usage

```java
import com.apideck.unify.models.components.Audience;

Audience value = Audience.APPLICATION_OWNER;

// Open enum: use .of() to create instances from custom string values
Audience custom = Audience.of("custom_value");
```


## Values

| Name                | Value               |
| ------------------- | ------------------- |
| `APPLICATION_OWNER` | application_owner   |
| `CONSUMER`          | consumer            |