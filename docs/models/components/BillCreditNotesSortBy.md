# BillCreditNotesSortBy

The field on which to sort the Bill Credit Notes

## Example Usage

```java
import com.apideck.unify.models.components.BillCreditNotesSortBy;

BillCreditNotesSortBy value = BillCreditNotesSortBy.CREATED_AT;

// Open enum: use .of() to create instances from custom string values
BillCreditNotesSortBy custom = BillCreditNotesSortBy.of("custom_value");
```


## Values

| Name         | Value        |
| ------------ | ------------ |
| `CREATED_AT` | created_at   |
| `UPDATED_AT` | updated_at   |
| `ID`         | id           |