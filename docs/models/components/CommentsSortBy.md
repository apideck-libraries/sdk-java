# CommentsSortBy

The field on which to sort the Comments

## Example Usage

```java
import com.apideck.unify.models.components.CommentsSortBy;

CommentsSortBy value = CommentsSortBy.CREATED_AT;

// Open enum: use .of() to create instances from custom string values
CommentsSortBy custom = CommentsSortBy.of("custom_value");
```


## Values

| Name         | Value        |
| ------------ | ------------ |
| `CREATED_AT` | created_at   |
| `UPDATED_AT` | updated_at   |