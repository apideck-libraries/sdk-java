# CreditNotesSortBy

The field on which to sort the Customers

## Example Usage

```java
import com.apideck.unify.models.components.CreditNotesSortBy;

CreditNotesSortBy value = CreditNotesSortBy.CREATED_AT;

// Open enum: use .of() to create instances from custom string values
CreditNotesSortBy custom = CreditNotesSortBy.of("custom_value");
```


## Values

| Name         | Value        |
| ------------ | ------------ |
| `CREATED_AT` | created_at   |
| `UPDATED_AT` | updated_at   |