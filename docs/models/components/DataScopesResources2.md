# DataScopesResources2

Wildcard indicating all resources and fields when Data Scopes is selected

## Example Usage

```java
import com.apideck.unify.models.components.DataScopesResources2;

DataScopesResources2 value = DataScopesResources2.WILDCARD;

// Open enum: use .of() to create instances from custom string values
DataScopesResources2 custom = DataScopesResources2.of("custom_value");
```


## Values

| Name       | Value      |
| ---------- | ---------- |
| `WILDCARD` | *          |