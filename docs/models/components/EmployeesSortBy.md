# EmployeesSortBy

The field on which to sort the Employees

## Example Usage

```java
import com.apideck.unify.models.components.EmployeesSortBy;

EmployeesSortBy value = EmployeesSortBy.FIRST_NAME;

// Open enum: use .of() to create instances from custom string values
EmployeesSortBy custom = EmployeesSortBy.of("custom_value");
```


## Values

| Name         | Value        |
| ------------ | ------------ |
| `FIRST_NAME` | first_name   |
| `LAST_NAME`  | last_name    |
| `CREATED_AT` | created_at   |
| `UPDATED_AT` | updated_at   |