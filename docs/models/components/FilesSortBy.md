# FilesSortBy

The field on which to sort the Files

## Example Usage

```java
import com.apideck.unify.models.components.FilesSortBy;

FilesSortBy value = FilesSortBy.CREATED_AT;

// Open enum: use .of() to create instances from custom string values
FilesSortBy custom = FilesSortBy.of("custom_value");
```


## Values

| Name         | Value        |
| ------------ | ------------ |
| `CREATED_AT` | created_at   |
| `UPDATED_AT` | updated_at   |
| `NAME`       | name         |