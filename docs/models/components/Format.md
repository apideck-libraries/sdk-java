# Format

Format of the doc.

## Example Usage

```java
import com.apideck.unify.models.components.Format;

Format value = Format.MARKDOWN;

// Open enum: use .of() to create instances from custom string values
Format custom = Format.of("custom_value");
```


## Values

| Name       | Value      |
| ---------- | ---------- |
| `MARKDOWN` | markdown   |