# PaymentsSortBy

The field on which to sort the Payments

## Example Usage

```java
import com.apideck.unify.models.components.PaymentsSortBy;

PaymentsSortBy value = PaymentsSortBy.UPDATED_AT;

// Open enum: use .of() to create instances from custom string values
PaymentsSortBy custom = PaymentsSortBy.of("custom_value");
```


## Values

| Name         | Value        |
| ------------ | ------------ |
| `UPDATED_AT` | updated_at   |
| `CREATED_AT` | created_at   |