# RefundsSortBy

The field on which to sort the Refunds

## Example Usage

```java
import com.apideck.unify.models.components.RefundsSortBy;

RefundsSortBy value = RefundsSortBy.UPDATED_AT;

// Open enum: use .of() to create instances from custom string values
RefundsSortBy custom = RefundsSortBy.of("custom_value");
```


## Values

| Name         | Value        |
| ------------ | ------------ |
| `UPDATED_AT` | updated_at   |
| `CREATED_AT` | created_at   |