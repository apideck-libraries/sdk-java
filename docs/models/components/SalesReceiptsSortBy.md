# SalesReceiptsSortBy

The field on which to sort the Sales Receipts.

## Example Usage

```java
import com.apideck.unify.models.components.SalesReceiptsSortBy;

SalesReceiptsSortBy value = SalesReceiptsSortBy.UPDATED_AT;

// Open enum: use .of() to create instances from custom string values
SalesReceiptsSortBy custom = SalesReceiptsSortBy.of("custom_value");
```


## Values

| Name         | Value        |
| ------------ | ------------ |
| `UPDATED_AT` | updated_at   |
| `CREATED_AT` | created_at   |