# Scope

The scope of the shared link.

## Example Usage

```java
import com.apideck.unify.models.components.Scope;

Scope value = Scope.PUBLIC;

// Open enum: use .of() to create instances from custom string values
Scope custom = Scope.of("custom_value");
```


## Values

| Name      | Value     |
| --------- | --------- |
| `PUBLIC`  | public    |
| `COMPANY` | company   |