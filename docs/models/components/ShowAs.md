# ShowAs

## Example Usage

```java
import com.apideck.unify.models.components.ShowAs;

ShowAs value = ShowAs.FREE;

// Open enum: use .of() to create instances from custom string values
ShowAs custom = ShowAs.of("custom_value");
```


## Values

| Name   | Value  |
| ------ | ------ |
| `FREE` | free   |
| `BUSY` | busy   |