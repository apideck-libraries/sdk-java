# TaxRateStatus

Tax rate status

## Example Usage

```java
import com.apideck.unify.models.components.TaxRateStatus;

TaxRateStatus value = TaxRateStatus.ACTIVE;

// Open enum: use .of() to create instances from custom string values
TaxRateStatus custom = TaxRateStatus.of("custom_value");
```


## Values

| Name       | Value      |
| ---------- | ---------- |
| `ACTIVE`   | active     |
| `INACTIVE` | inactive   |
| `ARCHIVED` | archived   |