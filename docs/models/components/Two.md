# Two

Wildcard indicating all resources and fields when Data Scopes is disabled

## Example Usage

```java
import com.apideck.unify.models.components.Two;

Two value = Two.WILDCARD;

// Open enum: use .of() to create instances from custom string values
Two custom = Two.of("custom_value");
```


## Values

| Name       | Value      |
| ---------- | ---------- |
| `WILDCARD` | *          |