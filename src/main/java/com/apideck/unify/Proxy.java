/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apideck.unify;

import static com.apideck.unify.operations.Operations.RequestOperation;

import com.apideck.unify.models.operations.ProxyDeleteProxyRequest;
import com.apideck.unify.models.operations.ProxyDeleteProxyRequestBuilder;
import com.apideck.unify.models.operations.ProxyDeleteProxyResponse;
import com.apideck.unify.models.operations.ProxyGetProxyRequest;
import com.apideck.unify.models.operations.ProxyGetProxyRequestBuilder;
import com.apideck.unify.models.operations.ProxyGetProxyResponse;
import com.apideck.unify.models.operations.ProxyOptionsProxyRequest;
import com.apideck.unify.models.operations.ProxyOptionsProxyRequestBuilder;
import com.apideck.unify.models.operations.ProxyOptionsProxyResponse;
import com.apideck.unify.models.operations.ProxyPatchProxyRequest;
import com.apideck.unify.models.operations.ProxyPatchProxyRequestBuilder;
import com.apideck.unify.models.operations.ProxyPatchProxyResponse;
import com.apideck.unify.models.operations.ProxyPostProxyRequest;
import com.apideck.unify.models.operations.ProxyPostProxyRequestBuilder;
import com.apideck.unify.models.operations.ProxyPostProxyResponse;
import com.apideck.unify.models.operations.ProxyPutProxyRequest;
import com.apideck.unify.models.operations.ProxyPutProxyRequestBuilder;
import com.apideck.unify.models.operations.ProxyPutProxyResponse;
import com.apideck.unify.operations.ProxyDeleteProxy;
import com.apideck.unify.operations.ProxyGetProxy;
import com.apideck.unify.operations.ProxyOptionsProxy;
import com.apideck.unify.operations.ProxyPatchProxy;
import com.apideck.unify.operations.ProxyPostProxy;
import com.apideck.unify.operations.ProxyPutProxy;
import com.apideck.unify.utils.Headers;
import com.apideck.unify.utils.Options;
import java.util.Optional;


public class Proxy {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncProxy asyncSDK;

    Proxy(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.asyncSDK = new AsyncProxy(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncProxy async() {
        return asyncSDK;
    }

    /**
     * GET
     * 
     * <p>Proxies a downstream GET request to a service and injects the necessary credentials into a request
     * stored in Vault. This allows you to have an additional security layer and logging without needing to
     * rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @return The call builder
     */
    public ProxyGetProxyRequestBuilder get() {
        return new ProxyGetProxyRequestBuilder(sdkConfiguration);
    }

    /**
     * GET
     * 
     * <p>Proxies a downstream GET request to a service and injects the necessary credentials into a request
     * stored in Vault. This allows you to have an additional security layer and logging without needing to
     * rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ProxyGetProxyResponse get(ProxyGetProxyRequest request) {
        return get(request, Optional.empty());
    }

    /**
     * GET
     * 
     * <p>Proxies a downstream GET request to a service and injects the necessary credentials into a request
     * stored in Vault. This allows you to have an additional security layer and logging without needing to
     * rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ProxyGetProxyResponse get(ProxyGetProxyRequest request, Optional<Options> options) {
        RequestOperation<ProxyGetProxyRequest, ProxyGetProxyResponse> operation
              = new ProxyGetProxy.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * OPTIONS
     * 
     * <p>Proxies a downstream OPTION request to a service and injects the necessary credentials into a
     * request stored in Vault. This allows you to have an additional security layer and logging without
     * needing to rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @return The call builder
     */
    public ProxyOptionsProxyRequestBuilder options() {
        return new ProxyOptionsProxyRequestBuilder(sdkConfiguration);
    }

    /**
     * OPTIONS
     * 
     * <p>Proxies a downstream OPTION request to a service and injects the necessary credentials into a
     * request stored in Vault. This allows you to have an additional security layer and logging without
     * needing to rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ProxyOptionsProxyResponse options(ProxyOptionsProxyRequest request) {
        return options(request, Optional.empty());
    }

    /**
     * OPTIONS
     * 
     * <p>Proxies a downstream OPTION request to a service and injects the necessary credentials into a
     * request stored in Vault. This allows you to have an additional security layer and logging without
     * needing to rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ProxyOptionsProxyResponse options(ProxyOptionsProxyRequest request, Optional<Options> options) {
        RequestOperation<ProxyOptionsProxyRequest, ProxyOptionsProxyResponse> operation
              = new ProxyOptionsProxy.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * POST
     * 
     * <p>Proxies a downstream POST request to a service and injects the necessary credentials into a request
     * stored in Vault. This allows you to have an additional security layer and logging without needing to
     * rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @return The call builder
     */
    public ProxyPostProxyRequestBuilder post() {
        return new ProxyPostProxyRequestBuilder(sdkConfiguration);
    }

    /**
     * POST
     * 
     * <p>Proxies a downstream POST request to a service and injects the necessary credentials into a request
     * stored in Vault. This allows you to have an additional security layer and logging without needing to
     * rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ProxyPostProxyResponse post(ProxyPostProxyRequest request) {
        return post(request, Optional.empty());
    }

    /**
     * POST
     * 
     * <p>Proxies a downstream POST request to a service and injects the necessary credentials into a request
     * stored in Vault. This allows you to have an additional security layer and logging without needing to
     * rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ProxyPostProxyResponse post(ProxyPostProxyRequest request, Optional<Options> options) {
        RequestOperation<ProxyPostProxyRequest, ProxyPostProxyResponse> operation
              = new ProxyPostProxy.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * PUT
     * 
     * <p>Proxies a downstream PUT request to a service and injects the necessary credentials into a request
     * stored in Vault. This allows you to have an additional security layer and logging without needing to
     * rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @return The call builder
     */
    public ProxyPutProxyRequestBuilder put() {
        return new ProxyPutProxyRequestBuilder(sdkConfiguration);
    }

    /**
     * PUT
     * 
     * <p>Proxies a downstream PUT request to a service and injects the necessary credentials into a request
     * stored in Vault. This allows you to have an additional security layer and logging without needing to
     * rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ProxyPutProxyResponse put(ProxyPutProxyRequest request) {
        return put(request, Optional.empty());
    }

    /**
     * PUT
     * 
     * <p>Proxies a downstream PUT request to a service and injects the necessary credentials into a request
     * stored in Vault. This allows you to have an additional security layer and logging without needing to
     * rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ProxyPutProxyResponse put(ProxyPutProxyRequest request, Optional<Options> options) {
        RequestOperation<ProxyPutProxyRequest, ProxyPutProxyResponse> operation
              = new ProxyPutProxy.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * PATCH
     * 
     * <p>Proxies a downstream PATCH request to a service and injects the necessary credentials into a request
     * stored in Vault. This allows you to have an additional security layer and logging without needing to
     * rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @return The call builder
     */
    public ProxyPatchProxyRequestBuilder patch() {
        return new ProxyPatchProxyRequestBuilder(sdkConfiguration);
    }

    /**
     * PATCH
     * 
     * <p>Proxies a downstream PATCH request to a service and injects the necessary credentials into a request
     * stored in Vault. This allows you to have an additional security layer and logging without needing to
     * rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ProxyPatchProxyResponse patch(ProxyPatchProxyRequest request) {
        return patch(request, Optional.empty());
    }

    /**
     * PATCH
     * 
     * <p>Proxies a downstream PATCH request to a service and injects the necessary credentials into a request
     * stored in Vault. This allows you to have an additional security layer and logging without needing to
     * rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ProxyPatchProxyResponse patch(ProxyPatchProxyRequest request, Optional<Options> options) {
        RequestOperation<ProxyPatchProxyRequest, ProxyPatchProxyResponse> operation
              = new ProxyPatchProxy.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * DELETE
     * 
     * <p>Proxies a downstream DELETE request to a service and injects the necessary credentials into a
     * request stored in Vault. This allows you to have an additional security layer and logging without
     * needing to rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @return The call builder
     */
    public ProxyDeleteProxyRequestBuilder delete() {
        return new ProxyDeleteProxyRequestBuilder(sdkConfiguration);
    }

    /**
     * DELETE
     * 
     * <p>Proxies a downstream DELETE request to a service and injects the necessary credentials into a
     * request stored in Vault. This allows you to have an additional security layer and logging without
     * needing to rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ProxyDeleteProxyResponse delete(ProxyDeleteProxyRequest request) {
        return delete(request, Optional.empty());
    }

    /**
     * DELETE
     * 
     * <p>Proxies a downstream DELETE request to a service and injects the necessary credentials into a
     * request stored in Vault. This allows you to have an additional security layer and logging without
     * needing to rely on Unify for normalization.
     * **Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ProxyDeleteProxyResponse delete(ProxyDeleteProxyRequest request, Optional<Options> options) {
        RequestOperation<ProxyDeleteProxyRequest, ProxyDeleteProxyResponse> operation
              = new ProxyDeleteProxy.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
