/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apideck.unify.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * Audience
 * 
 * <p>Audience for the doc.
 */
public class Audience {

    public static final Audience APPLICATION_OWNER = new Audience("application_owner");
    public static final Audience CONSUMER = new Audience("consumer");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, Audience> values = createValuesMap();
    private static final Map<String, AudienceEnum> enums = createEnumsMap();

    private final String value;

    private Audience(String value) {
        this.value = value;
    }

    /**
     * Returns a Audience with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as Audience
     */ 
    @JsonCreator
    public static Audience of(String value) {
        synchronized (Audience.class) {
            return values.computeIfAbsent(value, v -> new Audience(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<AudienceEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        Audience other = (Audience) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "Audience [value=" + value + "]";
    }

    // return an array just like an enum
    public static Audience[] values() {
        synchronized (Audience.class) {
            return values.values().toArray(new Audience[] {});
        }
    }

    private static final Map<String, Audience> createValuesMap() {
        Map<String, Audience> map = new LinkedHashMap<>();
        map.put("application_owner", APPLICATION_OWNER);
        map.put("consumer", CONSUMER);
        return map;
    }

    private static final Map<String, AudienceEnum> createEnumsMap() {
        Map<String, AudienceEnum> map = new HashMap<>();
        map.put("application_owner", AudienceEnum.APPLICATION_OWNER);
        map.put("consumer", AudienceEnum.CONSUMER);
        return map;
    }
    
    
    public enum AudienceEnum {

        APPLICATION_OWNER("application_owner"),
        CONSUMER("consumer"),;

        private final String value;

        private AudienceEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

