/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apideck.unify.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * BillCreditNoteType
 * 
 * <p>The type of credit note. A bill credit note is always an accounts payable (supplier-side) credit.
 */
public class BillCreditNoteType {

    public static final BillCreditNoteType ACCOUNTS_PAYABLE_CREDIT = new BillCreditNoteType("accounts_payable_credit");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, BillCreditNoteType> values = createValuesMap();
    private static final Map<String, BillCreditNoteTypeEnum> enums = createEnumsMap();

    private final String value;

    private BillCreditNoteType(String value) {
        this.value = value;
    }

    /**
     * Returns a BillCreditNoteType with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as BillCreditNoteType
     */ 
    @JsonCreator
    public static BillCreditNoteType of(String value) {
        synchronized (BillCreditNoteType.class) {
            return values.computeIfAbsent(value, v -> new BillCreditNoteType(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<BillCreditNoteTypeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        BillCreditNoteType other = (BillCreditNoteType) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "BillCreditNoteType [value=" + value + "]";
    }

    // return an array just like an enum
    public static BillCreditNoteType[] values() {
        synchronized (BillCreditNoteType.class) {
            return values.values().toArray(new BillCreditNoteType[] {});
        }
    }

    private static final Map<String, BillCreditNoteType> createValuesMap() {
        Map<String, BillCreditNoteType> map = new LinkedHashMap<>();
        map.put("accounts_payable_credit", ACCOUNTS_PAYABLE_CREDIT);
        return map;
    }

    private static final Map<String, BillCreditNoteTypeEnum> createEnumsMap() {
        Map<String, BillCreditNoteTypeEnum> map = new HashMap<>();
        map.put("accounts_payable_credit", BillCreditNoteTypeEnum.ACCOUNTS_PAYABLE_CREDIT);
        return map;
    }
    
    
    public enum BillCreditNoteTypeEnum {

        ACCOUNTS_PAYABLE_CREDIT("accounts_payable_credit"),;

        private final String value;

        private BillCreditNoteTypeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

