/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apideck.unify.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * ConnectionHealth
 * 
 * <p>The operational health status of the connection
 */
public class ConnectionHealth {

    public static final ConnectionHealth OK = new ConnectionHealth("ok");
    public static final ConnectionHealth PENDING_REFRESH = new ConnectionHealth("pending_refresh");
    public static final ConnectionHealth NEEDS_AUTH = new ConnectionHealth("needs_auth");
    public static final ConnectionHealth NEEDS_CONSENT = new ConnectionHealth("needs_consent");
    public static final ConnectionHealth REVOKED = new ConnectionHealth("revoked");
    public static final ConnectionHealth MISSING_SETTINGS = new ConnectionHealth("missing_settings");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ConnectionHealth> values = createValuesMap();
    private static final Map<String, ConnectionHealthEnum> enums = createEnumsMap();

    private final String value;

    private ConnectionHealth(String value) {
        this.value = value;
    }

    /**
     * Returns a ConnectionHealth with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ConnectionHealth
     */ 
    @JsonCreator
    public static ConnectionHealth of(String value) {
        synchronized (ConnectionHealth.class) {
            return values.computeIfAbsent(value, v -> new ConnectionHealth(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ConnectionHealthEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ConnectionHealth other = (ConnectionHealth) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ConnectionHealth [value=" + value + "]";
    }

    // return an array just like an enum
    public static ConnectionHealth[] values() {
        synchronized (ConnectionHealth.class) {
            return values.values().toArray(new ConnectionHealth[] {});
        }
    }

    private static final Map<String, ConnectionHealth> createValuesMap() {
        Map<String, ConnectionHealth> map = new LinkedHashMap<>();
        map.put("ok", OK);
        map.put("pending_refresh", PENDING_REFRESH);
        map.put("needs_auth", NEEDS_AUTH);
        map.put("needs_consent", NEEDS_CONSENT);
        map.put("revoked", REVOKED);
        map.put("missing_settings", MISSING_SETTINGS);
        return map;
    }

    private static final Map<String, ConnectionHealthEnum> createEnumsMap() {
        Map<String, ConnectionHealthEnum> map = new HashMap<>();
        map.put("ok", ConnectionHealthEnum.OK);
        map.put("pending_refresh", ConnectionHealthEnum.PENDING_REFRESH);
        map.put("needs_auth", ConnectionHealthEnum.NEEDS_AUTH);
        map.put("needs_consent", ConnectionHealthEnum.NEEDS_CONSENT);
        map.put("revoked", ConnectionHealthEnum.REVOKED);
        map.put("missing_settings", ConnectionHealthEnum.MISSING_SETTINGS);
        return map;
    }
    
    
    public enum ConnectionHealthEnum {

        OK("ok"),
        PENDING_REFRESH("pending_refresh"),
        NEEDS_AUTH("needs_auth"),
        NEEDS_CONSENT("needs_consent"),
        REVOKED("revoked"),
        MISSING_SETTINGS("missing_settings"),;

        private final String value;

        private ConnectionHealthEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

