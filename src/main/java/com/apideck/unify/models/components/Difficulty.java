/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apideck.unify.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * Difficulty
 * 
 * <p>At-a-glance implementation difficulty rating.
 */
public class Difficulty {

    public static final Difficulty STRAIGHTFORWARD = new Difficulty("straightforward");
    public static final Difficulty MODERATE = new Difficulty("moderate");
    public static final Difficulty INVOLVED = new Difficulty("involved");
    public static final Difficulty HIGHLY_COMPLEX = new Difficulty("highly_complex");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, Difficulty> values = createValuesMap();
    private static final Map<String, DifficultyEnum> enums = createEnumsMap();

    private final String value;

    private Difficulty(String value) {
        this.value = value;
    }

    /**
     * Returns a Difficulty with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as Difficulty
     */ 
    @JsonCreator
    public static Difficulty of(String value) {
        synchronized (Difficulty.class) {
            return values.computeIfAbsent(value, v -> new Difficulty(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<DifficultyEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        Difficulty other = (Difficulty) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "Difficulty [value=" + value + "]";
    }

    // return an array just like an enum
    public static Difficulty[] values() {
        synchronized (Difficulty.class) {
            return values.values().toArray(new Difficulty[] {});
        }
    }

    private static final Map<String, Difficulty> createValuesMap() {
        Map<String, Difficulty> map = new LinkedHashMap<>();
        map.put("straightforward", STRAIGHTFORWARD);
        map.put("moderate", MODERATE);
        map.put("involved", INVOLVED);
        map.put("highly_complex", HIGHLY_COMPLEX);
        return map;
    }

    private static final Map<String, DifficultyEnum> createEnumsMap() {
        Map<String, DifficultyEnum> map = new HashMap<>();
        map.put("straightforward", DifficultyEnum.STRAIGHTFORWARD);
        map.put("moderate", DifficultyEnum.MODERATE);
        map.put("involved", DifficultyEnum.INVOLVED);
        map.put("highly_complex", DifficultyEnum.HIGHLY_COMPLEX);
        return map;
    }
    
    
    public enum DifficultyEnum {

        STRAIGHTFORWARD("straightforward"),
        MODERATE("moderate"),
        INVOLVED("involved"),
        HIGHLY_COMPLEX("highly_complex"),;

        private final String value;

        private DifficultyEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

