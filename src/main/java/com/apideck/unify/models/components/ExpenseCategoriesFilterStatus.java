/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apideck.unify.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ExpenseCategoriesFilterStatus {

    public static final ExpenseCategoriesFilterStatus ACTIVE = new ExpenseCategoriesFilterStatus("active");
    public static final ExpenseCategoriesFilterStatus INACTIVE = new ExpenseCategoriesFilterStatus("inactive");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ExpenseCategoriesFilterStatus> values = createValuesMap();
    private static final Map<String, ExpenseCategoriesFilterStatusEnum> enums = createEnumsMap();

    private final String value;

    private ExpenseCategoriesFilterStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a ExpenseCategoriesFilterStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ExpenseCategoriesFilterStatus
     */ 
    @JsonCreator
    public static ExpenseCategoriesFilterStatus of(String value) {
        synchronized (ExpenseCategoriesFilterStatus.class) {
            return values.computeIfAbsent(value, v -> new ExpenseCategoriesFilterStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ExpenseCategoriesFilterStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ExpenseCategoriesFilterStatus other = (ExpenseCategoriesFilterStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ExpenseCategoriesFilterStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static ExpenseCategoriesFilterStatus[] values() {
        synchronized (ExpenseCategoriesFilterStatus.class) {
            return values.values().toArray(new ExpenseCategoriesFilterStatus[] {});
        }
    }

    private static final Map<String, ExpenseCategoriesFilterStatus> createValuesMap() {
        Map<String, ExpenseCategoriesFilterStatus> map = new LinkedHashMap<>();
        map.put("active", ACTIVE);
        map.put("inactive", INACTIVE);
        return map;
    }

    private static final Map<String, ExpenseCategoriesFilterStatusEnum> createEnumsMap() {
        Map<String, ExpenseCategoriesFilterStatusEnum> map = new HashMap<>();
        map.put("active", ExpenseCategoriesFilterStatusEnum.ACTIVE);
        map.put("inactive", ExpenseCategoriesFilterStatusEnum.INACTIVE);
        return map;
    }
    
    
    public enum ExpenseCategoriesFilterStatusEnum {

        ACTIVE("active"),
        INACTIVE("inactive"),;

        private final String value;

        private ExpenseCategoriesFilterStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

