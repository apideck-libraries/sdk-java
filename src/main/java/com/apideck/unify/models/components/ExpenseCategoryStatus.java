/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apideck.unify.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * ExpenseCategoryStatus
 * 
 * <p>The status of the expense category.
 */
public class ExpenseCategoryStatus {

    public static final ExpenseCategoryStatus ACTIVE = new ExpenseCategoryStatus("active");
    public static final ExpenseCategoryStatus INACTIVE = new ExpenseCategoryStatus("inactive");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ExpenseCategoryStatus> values = createValuesMap();
    private static final Map<String, ExpenseCategoryStatusEnum> enums = createEnumsMap();

    private final String value;

    private ExpenseCategoryStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a ExpenseCategoryStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ExpenseCategoryStatus
     */ 
    @JsonCreator
    public static ExpenseCategoryStatus of(String value) {
        synchronized (ExpenseCategoryStatus.class) {
            return values.computeIfAbsent(value, v -> new ExpenseCategoryStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ExpenseCategoryStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ExpenseCategoryStatus other = (ExpenseCategoryStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ExpenseCategoryStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static ExpenseCategoryStatus[] values() {
        synchronized (ExpenseCategoryStatus.class) {
            return values.values().toArray(new ExpenseCategoryStatus[] {});
        }
    }

    private static final Map<String, ExpenseCategoryStatus> createValuesMap() {
        Map<String, ExpenseCategoryStatus> map = new LinkedHashMap<>();
        map.put("active", ACTIVE);
        map.put("inactive", INACTIVE);
        return map;
    }

    private static final Map<String, ExpenseCategoryStatusEnum> createEnumsMap() {
        Map<String, ExpenseCategoryStatusEnum> map = new HashMap<>();
        map.put("active", ExpenseCategoryStatusEnum.ACTIVE);
        map.put("inactive", ExpenseCategoryStatusEnum.INACTIVE);
        return map;
    }
    
    
    public enum ExpenseCategoryStatusEnum {

        ACTIVE("active"),
        INACTIVE("inactive"),;

        private final String value;

        private ExpenseCategoryStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

