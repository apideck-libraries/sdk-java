/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apideck.unify.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * JournalEntriesFilterScope
 * 
 * <p>Connector-specific scope hint that controls which downstream source backs the read. On Xero,
 * `manual` reads from `ManualJournals` (free in every tier), while `system` reads from `Journals` (the
 * full general ledger view including manual journal postings, paid post 2026-03-02). Omitting the
 * filter is equivalent to `system` and preserves the legacy default.
 * 
 * <p>Only honored on connectors where the distinction is exposed; ignored elsewhere.
 */
public class JournalEntriesFilterScope {

    public static final JournalEntriesFilterScope MANUAL = new JournalEntriesFilterScope("manual");
    public static final JournalEntriesFilterScope SYSTEM = new JournalEntriesFilterScope("system");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, JournalEntriesFilterScope> values = createValuesMap();
    private static final Map<String, JournalEntriesFilterScopeEnum> enums = createEnumsMap();

    private final String value;

    private JournalEntriesFilterScope(String value) {
        this.value = value;
    }

    /**
     * Returns a JournalEntriesFilterScope with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as JournalEntriesFilterScope
     */ 
    @JsonCreator
    public static JournalEntriesFilterScope of(String value) {
        synchronized (JournalEntriesFilterScope.class) {
            return values.computeIfAbsent(value, v -> new JournalEntriesFilterScope(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<JournalEntriesFilterScopeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        JournalEntriesFilterScope other = (JournalEntriesFilterScope) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "JournalEntriesFilterScope [value=" + value + "]";
    }

    // return an array just like an enum
    public static JournalEntriesFilterScope[] values() {
        synchronized (JournalEntriesFilterScope.class) {
            return values.values().toArray(new JournalEntriesFilterScope[] {});
        }
    }

    private static final Map<String, JournalEntriesFilterScope> createValuesMap() {
        Map<String, JournalEntriesFilterScope> map = new LinkedHashMap<>();
        map.put("manual", MANUAL);
        map.put("system", SYSTEM);
        return map;
    }

    private static final Map<String, JournalEntriesFilterScopeEnum> createEnumsMap() {
        Map<String, JournalEntriesFilterScopeEnum> map = new HashMap<>();
        map.put("manual", JournalEntriesFilterScopeEnum.MANUAL);
        map.put("system", JournalEntriesFilterScopeEnum.SYSTEM);
        return map;
    }
    
    
    public enum JournalEntriesFilterScopeEnum {

        MANUAL("manual"),
        SYSTEM("system"),;

        private final String value;

        private JournalEntriesFilterScopeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

