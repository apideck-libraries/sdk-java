/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apideck.unify.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * LedgerAccountsFilterStatus
 * 
 * <p>Filter by account status. Supported only on a subset of connectors (e.g. NetSuite); connectors that
 * do not support it reject `filter[status]` with a `400 UnsupportedFiltersError` — read the account's
 * `status` field in the response and filter client-side instead.
 * 
 * <p>See the error's `supported_filters` or the connector's supported filters.
 */
public class LedgerAccountsFilterStatus {

    public static final LedgerAccountsFilterStatus ACTIVE = new LedgerAccountsFilterStatus("active");
    public static final LedgerAccountsFilterStatus INACTIVE = new LedgerAccountsFilterStatus("inactive");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, LedgerAccountsFilterStatus> values = createValuesMap();
    private static final Map<String, LedgerAccountsFilterStatusEnum> enums = createEnumsMap();

    private final String value;

    private LedgerAccountsFilterStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a LedgerAccountsFilterStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as LedgerAccountsFilterStatus
     */ 
    @JsonCreator
    public static LedgerAccountsFilterStatus of(String value) {
        synchronized (LedgerAccountsFilterStatus.class) {
            return values.computeIfAbsent(value, v -> new LedgerAccountsFilterStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<LedgerAccountsFilterStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        LedgerAccountsFilterStatus other = (LedgerAccountsFilterStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "LedgerAccountsFilterStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static LedgerAccountsFilterStatus[] values() {
        synchronized (LedgerAccountsFilterStatus.class) {
            return values.values().toArray(new LedgerAccountsFilterStatus[] {});
        }
    }

    private static final Map<String, LedgerAccountsFilterStatus> createValuesMap() {
        Map<String, LedgerAccountsFilterStatus> map = new LinkedHashMap<>();
        map.put("active", ACTIVE);
        map.put("inactive", INACTIVE);
        return map;
    }

    private static final Map<String, LedgerAccountsFilterStatusEnum> createEnumsMap() {
        Map<String, LedgerAccountsFilterStatusEnum> map = new HashMap<>();
        map.put("active", LedgerAccountsFilterStatusEnum.ACTIVE);
        map.put("inactive", LedgerAccountsFilterStatusEnum.INACTIVE);
        return map;
    }
    
    
    public enum LedgerAccountsFilterStatusEnum {

        ACTIVE("active"),
        INACTIVE("inactive"),;

        private final String value;

        private LedgerAccountsFilterStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

