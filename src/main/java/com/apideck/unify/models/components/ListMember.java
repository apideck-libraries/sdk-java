/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apideck.unify.models.components;

import com.apideck.unify.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.lang.Override;
import java.lang.String;
import org.openapitools.jackson.nullable.JsonNullable;


public class ListMember {
    /**
     * The unique identifier of the record that belongs to the List, as returned by the downstream
     * connector.
     */
    @JsonProperty("id")
    private String id;

    /**
     * The type of CRM record this member is. Normalized to a lowercase plural noun for the vendor's
     * standard object types (e.g. contacts, companies, opportunities, leads, tickets, campaigns, tasks,
     * events); for vendor-specific or custom object types the raw downstream identifier is passed through
     * unchanged, mirroring the semantics of `List.object_type`.
     * 
     * <p>This field is intentionally open-ended — no closed enum is enforced.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("object_type")
    private JsonNullable<String> objectType;

    @JsonCreator
    public ListMember(
            @JsonProperty("id") String id,
            @JsonProperty("object_type") JsonNullable<String> objectType) {
        Utils.checkNotNull(id, "id");
        Utils.checkNotNull(objectType, "objectType");
        this.id = id;
        this.objectType = objectType;
    }
    
    public ListMember(
            String id) {
        this(id, JsonNullable.undefined());
    }

    /**
     * The unique identifier of the record that belongs to the List, as returned by the downstream
     * connector.
     */
    @JsonIgnore
    public String id() {
        return id;
    }

    /**
     * The type of CRM record this member is. Normalized to a lowercase plural noun for the vendor's
     * standard object types (e.g. contacts, companies, opportunities, leads, tickets, campaigns, tasks,
     * events); for vendor-specific or custom object types the raw downstream identifier is passed through
     * unchanged, mirroring the semantics of `List.object_type`.
     * 
     * <p>This field is intentionally open-ended — no closed enum is enforced.
     */
    @JsonIgnore
    public JsonNullable<String> objectType() {
        return objectType;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * The unique identifier of the record that belongs to the List, as returned by the downstream
     * connector.
     */
    public ListMember withId(String id) {
        Utils.checkNotNull(id, "id");
        this.id = id;
        return this;
    }

    /**
     * The type of CRM record this member is. Normalized to a lowercase plural noun for the vendor's
     * standard object types (e.g. contacts, companies, opportunities, leads, tickets, campaigns, tasks,
     * events); for vendor-specific or custom object types the raw downstream identifier is passed through
     * unchanged, mirroring the semantics of `List.object_type`.
     * 
     * <p>This field is intentionally open-ended — no closed enum is enforced.
     */
    public ListMember withObjectType(String objectType) {
        Utils.checkNotNull(objectType, "objectType");
        this.objectType = JsonNullable.of(objectType);
        return this;
    }

    /**
     * The type of CRM record this member is. Normalized to a lowercase plural noun for the vendor's
     * standard object types (e.g. contacts, companies, opportunities, leads, tickets, campaigns, tasks,
     * events); for vendor-specific or custom object types the raw downstream identifier is passed through
     * unchanged, mirroring the semantics of `List.object_type`.
     * 
     * <p>This field is intentionally open-ended — no closed enum is enforced.
     */
    public ListMember withObjectType(JsonNullable<String> objectType) {
        Utils.checkNotNull(objectType, "objectType");
        this.objectType = objectType;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ListMember other = (ListMember) o;
        return 
            Utils.enhancedDeepEquals(this.id, other.id) &&
            Utils.enhancedDeepEquals(this.objectType, other.objectType);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            id, objectType);
    }
    
    @Override
    public String toString() {
        return Utils.toString(ListMember.class,
                "id", id,
                "objectType", objectType);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String id;

        private JsonNullable<String> objectType = JsonNullable.undefined();

        private Builder() {
          // force use of static builder() method
        }


        /**
         * The unique identifier of the record that belongs to the List, as returned by the downstream
         * connector.
         */
        public Builder id(String id) {
            Utils.checkNotNull(id, "id");
            this.id = id;
            return this;
        }


        /**
         * The type of CRM record this member is. Normalized to a lowercase plural noun for the vendor's
         * standard object types (e.g. contacts, companies, opportunities, leads, tickets, campaigns, tasks,
         * events); for vendor-specific or custom object types the raw downstream identifier is passed through
         * unchanged, mirroring the semantics of `List.object_type`.
         * 
         * <p>This field is intentionally open-ended — no closed enum is enforced.
         */
        public Builder objectType(String objectType) {
            Utils.checkNotNull(objectType, "objectType");
            this.objectType = JsonNullable.of(objectType);
            return this;
        }

        /**
         * The type of CRM record this member is. Normalized to a lowercase plural noun for the vendor's
         * standard object types (e.g. contacts, companies, opportunities, leads, tickets, campaigns, tasks,
         * events); for vendor-specific or custom object types the raw downstream identifier is passed through
         * unchanged, mirroring the semantics of `List.object_type`.
         * 
         * <p>This field is intentionally open-ended — no closed enum is enforced.
         */
        public Builder objectType(JsonNullable<String> objectType) {
            Utils.checkNotNull(objectType, "objectType");
            this.objectType = objectType;
            return this;
        }

        public ListMember build() {

            return new ListMember(
                id, objectType);
        }

    }
}
