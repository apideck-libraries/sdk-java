/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apideck.unify.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * ListType
 * 
 * <p>Whether the List is static (a fixed set of records) or dynamic (a saved segment that is
 * automatically kept up to date based on filter criteria).
 */
public class ListType {

    public static final ListType STATIC = new ListType("static");
    public static final ListType DYNAMIC = new ListType("dynamic");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListType> values = createValuesMap();
    private static final Map<String, ListTypeEnum> enums = createEnumsMap();

    private final String value;

    private ListType(String value) {
        this.value = value;
    }

    /**
     * Returns a ListType with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListType
     */ 
    @JsonCreator
    public static ListType of(String value) {
        synchronized (ListType.class) {
            return values.computeIfAbsent(value, v -> new ListType(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListTypeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListType other = (ListType) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListType [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListType[] values() {
        synchronized (ListType.class) {
            return values.values().toArray(new ListType[] {});
        }
    }

    private static final Map<String, ListType> createValuesMap() {
        Map<String, ListType> map = new LinkedHashMap<>();
        map.put("static", STATIC);
        map.put("dynamic", DYNAMIC);
        return map;
    }

    private static final Map<String, ListTypeEnum> createEnumsMap() {
        Map<String, ListTypeEnum> map = new HashMap<>();
        map.put("static", ListTypeEnum.STATIC);
        map.put("dynamic", ListTypeEnum.DYNAMIC);
        return map;
    }
    
    
    public enum ListTypeEnum {

        STATIC("static"),
        DYNAMIC("dynamic"),;

        private final String value;

        private ListTypeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

