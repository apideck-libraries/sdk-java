/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apideck.unify.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * ListVisibility
 * 
 * <p>The visibility of the List. Which of these values a given connector can return depends on its native
 * sharing model — see the connector-specific gotchas below for details.
 */
public class ListVisibility {

    public static final ListVisibility PRIVATE = new ListVisibility("private");
    public static final ListVisibility SHARED = new ListVisibility("shared");
    public static final ListVisibility PUBLIC = new ListVisibility("public");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListVisibility> values = createValuesMap();
    private static final Map<String, ListVisibilityEnum> enums = createEnumsMap();

    private final String value;

    private ListVisibility(String value) {
        this.value = value;
    }

    /**
     * Returns a ListVisibility with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListVisibility
     */ 
    @JsonCreator
    public static ListVisibility of(String value) {
        synchronized (ListVisibility.class) {
            return values.computeIfAbsent(value, v -> new ListVisibility(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListVisibilityEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListVisibility other = (ListVisibility) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListVisibility [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListVisibility[] values() {
        synchronized (ListVisibility.class) {
            return values.values().toArray(new ListVisibility[] {});
        }
    }

    private static final Map<String, ListVisibility> createValuesMap() {
        Map<String, ListVisibility> map = new LinkedHashMap<>();
        map.put("private", PRIVATE);
        map.put("shared", SHARED);
        map.put("public", PUBLIC);
        return map;
    }

    private static final Map<String, ListVisibilityEnum> createEnumsMap() {
        Map<String, ListVisibilityEnum> map = new HashMap<>();
        map.put("private", ListVisibilityEnum.PRIVATE);
        map.put("shared", ListVisibilityEnum.SHARED);
        map.put("public", ListVisibilityEnum.PUBLIC);
        return map;
    }
    
    
    public enum ListVisibilityEnum {

        PRIVATE("private"),
        SHARED("shared"),
        PUBLIC("public"),;

        private final String value;

        private ListVisibilityEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

