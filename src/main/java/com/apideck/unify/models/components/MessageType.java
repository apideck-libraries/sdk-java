/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apideck.unify.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * MessageType
 * 
 * <p>Set to sms for SMS messages and mms for MMS messages.
 */
public class MessageType {

    public static final MessageType SMS = new MessageType("sms");
    public static final MessageType MMS = new MessageType("mms");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, MessageType> values = createValuesMap();
    private static final Map<String, MessageTypeEnum> enums = createEnumsMap();

    private final String value;

    private MessageType(String value) {
        this.value = value;
    }

    /**
     * Returns a MessageType with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as MessageType
     */ 
    @JsonCreator
    public static MessageType of(String value) {
        synchronized (MessageType.class) {
            return values.computeIfAbsent(value, v -> new MessageType(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<MessageTypeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        MessageType other = (MessageType) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "MessageType [value=" + value + "]";
    }

    // return an array just like an enum
    public static MessageType[] values() {
        synchronized (MessageType.class) {
            return values.values().toArray(new MessageType[] {});
        }
    }

    private static final Map<String, MessageType> createValuesMap() {
        Map<String, MessageType> map = new LinkedHashMap<>();
        map.put("sms", SMS);
        map.put("mms", MMS);
        return map;
    }

    private static final Map<String, MessageTypeEnum> createEnumsMap() {
        Map<String, MessageTypeEnum> map = new HashMap<>();
        map.put("sms", MessageTypeEnum.SMS);
        map.put("mms", MessageTypeEnum.MMS);
        return map;
    }
    
    
    public enum MessageTypeEnum {

        SMS("sms"),
        MMS("mms"),;

        private final String value;

        private MessageTypeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

