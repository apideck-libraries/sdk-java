/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apideck.unify.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ShowAs {

    public static final ShowAs FREE = new ShowAs("free");
    public static final ShowAs BUSY = new ShowAs("busy");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ShowAs> values = createValuesMap();
    private static final Map<String, ShowAsEnum> enums = createEnumsMap();

    private final String value;

    private ShowAs(String value) {
        this.value = value;
    }

    /**
     * Returns a ShowAs with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ShowAs
     */ 
    @JsonCreator
    public static ShowAs of(String value) {
        synchronized (ShowAs.class) {
            return values.computeIfAbsent(value, v -> new ShowAs(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ShowAsEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ShowAs other = (ShowAs) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ShowAs [value=" + value + "]";
    }

    // return an array just like an enum
    public static ShowAs[] values() {
        synchronized (ShowAs.class) {
            return values.values().toArray(new ShowAs[] {});
        }
    }

    private static final Map<String, ShowAs> createValuesMap() {
        Map<String, ShowAs> map = new LinkedHashMap<>();
        map.put("free", FREE);
        map.put("busy", BUSY);
        return map;
    }

    private static final Map<String, ShowAsEnum> createEnumsMap() {
        Map<String, ShowAsEnum> map = new HashMap<>();
        map.put("free", ShowAsEnum.FREE);
        map.put("busy", ShowAsEnum.BUSY);
        return map;
    }
    
    
    public enum ShowAsEnum {

        FREE("free"),
        BUSY("busy"),;

        private final String value;

        private ShowAsEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

