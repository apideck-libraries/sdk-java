/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apideck.unify.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * TaxStatus
 * 
 * <p>The tax applicability of the product: `taxable` (the product is taxed), `shipping` (only the
 * shipping is taxed, the product itself is exempt) or `none` (neither is taxed).
 */
public class TaxStatus {

    public static final TaxStatus TAXABLE = new TaxStatus("taxable");
    public static final TaxStatus SHIPPING = new TaxStatus("shipping");
    public static final TaxStatus NONE = new TaxStatus("none");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, TaxStatus> values = createValuesMap();
    private static final Map<String, TaxStatusEnum> enums = createEnumsMap();

    private final String value;

    private TaxStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a TaxStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as TaxStatus
     */ 
    @JsonCreator
    public static TaxStatus of(String value) {
        synchronized (TaxStatus.class) {
            return values.computeIfAbsent(value, v -> new TaxStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<TaxStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        TaxStatus other = (TaxStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "TaxStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static TaxStatus[] values() {
        synchronized (TaxStatus.class) {
            return values.values().toArray(new TaxStatus[] {});
        }
    }

    private static final Map<String, TaxStatus> createValuesMap() {
        Map<String, TaxStatus> map = new LinkedHashMap<>();
        map.put("taxable", TAXABLE);
        map.put("shipping", SHIPPING);
        map.put("none", NONE);
        return map;
    }

    private static final Map<String, TaxStatusEnum> createEnumsMap() {
        Map<String, TaxStatusEnum> map = new HashMap<>();
        map.put("taxable", TaxStatusEnum.TAXABLE);
        map.put("shipping", TaxStatusEnum.SHIPPING);
        map.put("none", TaxStatusEnum.NONE);
        return map;
    }
    
    
    public enum TaxStatusEnum {

        TAXABLE("taxable"),
        SHIPPING("shipping"),
        NONE("none"),;

        private final String value;

        private TaxStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

