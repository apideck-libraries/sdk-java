/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apideck.unify.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * TaxType
 * 
 * <p>The tax applicability of this line item. Overrides the root-level tax_type for this line.
 */
public class TaxType {

    public static final TaxType SALES = new TaxType("sales");
    public static final TaxType PURCHASE = new TaxType("purchase");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, TaxType> values = createValuesMap();
    private static final Map<String, TaxTypeEnum> enums = createEnumsMap();

    private final String value;

    private TaxType(String value) {
        this.value = value;
    }

    /**
     * Returns a TaxType with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as TaxType
     */ 
    @JsonCreator
    public static TaxType of(String value) {
        synchronized (TaxType.class) {
            return values.computeIfAbsent(value, v -> new TaxType(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<TaxTypeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        TaxType other = (TaxType) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "TaxType [value=" + value + "]";
    }

    // return an array just like an enum
    public static TaxType[] values() {
        synchronized (TaxType.class) {
            return values.values().toArray(new TaxType[] {});
        }
    }

    private static final Map<String, TaxType> createValuesMap() {
        Map<String, TaxType> map = new LinkedHashMap<>();
        map.put("sales", SALES);
        map.put("purchase", PURCHASE);
        return map;
    }

    private static final Map<String, TaxTypeEnum> createEnumsMap() {
        Map<String, TaxTypeEnum> map = new HashMap<>();
        map.put("sales", TaxTypeEnum.SALES);
        map.put("purchase", TaxTypeEnum.PURCHASE);
        return map;
    }
    
    
    public enum TaxTypeEnum {

        SALES("sales"),
        PURCHASE("purchase"),;

        private final String value;

        private TaxTypeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

